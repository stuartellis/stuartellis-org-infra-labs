stages:
- review

# variables:
#   ENVIRONMENT: dev
#   PRODUCT_NAME: labs
#   SUBSYSTEM_NAME: canary
#   STACK_NAME: infra-one
#   STACK_VARIANT: default
#   RELEASE_VERSION: 0.2.0

review_canary:
  stage: review
  variables:
    PRODUCT_NAME: labs
    SUBSYSTEM_NAME: canary
    STACK_VARIANT: $CI_COMMIT_REF_SLUG
  trigger:
    include: .gitlab/ci/workflows/subsystems/canary/review/.gitlab-ci.yml
  only:
    - branches
  except:
    - main
